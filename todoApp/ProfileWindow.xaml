<Window x:Class="todoApp.ProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Todo Calendar"
        Width="800" Height="450"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        Background="Transparent">

    <Border CornerRadius="20" Background="#1F1F1F" Padding="20">
        <Grid>

            <!-- Gradient -->
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#141414" Offset="0"/>
                    <GradientStop Color="#1F1F1F" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>

            <!-- Close Button -->
            <Button x:Name="CloseBtn"
                    Content="✕"
                    FontSize="16"
                    Width="36" Height="36"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0,10,10,0"
                    Foreground="White"
                    Cursor="Hand"
                    Click="CloseBtn_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#2A2A2A"/>
                        <Setter Property="BorderBrush" Value="#444444"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E81123"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- Content -->
            <StackPanel VerticalAlignment="Center"
                        HorizontalAlignment="Center">

                <TextBlock x:Name="TitleText"
                           Text="Who's planning today?"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,40"/>

                <StackPanel x:Name="ProfilesRow"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"/>

            </StackPanel>

        </Grid>
    </Border>
</Window>